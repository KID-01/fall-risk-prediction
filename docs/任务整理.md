# 跌倒风险预测系统 - 任务完成状态对照表

> 对照依据：`更新日志.md`（更新至 2026-08-03）  
> 统计时间：2026-08-03（经源码核查后修正）  
> 任务总数：43  
> ✅ 已完成：20 | 🔄 部分完成：11 | ❌ 未开始：12

> ⚠️ 修正说明（2026-08-03）：本次对照项目源码逐项核查，更新如下：
> 1. **T1.3 萤石SDK**：代码已就绪（`src/ezviz/client.py` 501行 EzvizClient + `scripts/test_ezviz.py`），Token 获取已验证通过，`configs/ezviz.yaml` 已含真实密钥（gitignore）。待设备激活后即可拉流。
> 2. **T3.1 YOLO-Pose 已落地**：`yolo_pose_extractor.py`、`create_keypoint_extractor` 工厂、`COCO_TO_MEDIAPIPE` 映射、`convert_coco_to_mediapipe`、`base.yaml` backend 字段均已实现，14 个测试通过。从"部分完成"升级为"已完成"。
> 3. **T2.4 标注工具已完成**：`docs/labelstudio.md` + 3 个脚本 + 46 个测试。从"未开始"升级为"已完成"。
> 4. **T2.6 DVC 已完成**：`.dvc/` + `data.dvc` + `docs/dvc.md` + dvc push 验证通过。从"未开始"升级为"已完成"。
> 5. **T3.3 特征验证已完成**：`scripts/validate_features.py` + 18 个测试 + 3 张图。从"未开始"升级为"已完成"。
> 6. **T8.3 持久化已加固**：monitor.py 阶段8 调用 `insert_risk_record`/`insert_alert_event`，含异常处理。
> 7. **T5.1~T5.5/T8.2 模型相关仍为部分完成**：代码就绪但无训练权重，`checkpoints/` 目录不存在。
> 8. **T11.2/T11.3/T11.4 仍为部分完成**：集成测试仅合成数据、无性能基准脚本、Docker 未实际运行验证。

---

## 一、已完成任务（20个）

> 说明：以下任务经源码核查，文件/类/函数确实存在且功能与描述相符。但"已完成"仅指代码就绪，不代表整系统可用（见第四节提醒）。

| 任务ID | 任务名称 | 所属模块 | 负责人 | 任务细节 | 核查证据 |
|--------|---------|---------|--------|---------|---------|
| T1.1 | 项目仓库初始化与目录结构 | 项目初始化与基础架构 | — | 建立 src-layout 项目结构（src/data、inference、models、alerts、api、edge、utils、ezviz），配置 pyproject.toml 依赖声明、.gitignore、Makefile 常用命令 | pyproject.toml、.gitignore、Makefile、目录结构存在 |
| T1.2 | 开发环境与GPU环境配置 | 项目初始化与基础架构 | — | Python 3.10+ 虚拟环境，安装 torch/fastapi/mediapipe/ultralytics/omegaconf/loguru/httpx 等依赖，编写环境检查脚本 | scripts/check_env.py 存在 |
| T1.3 | 萤石开放平台 SDK 集成 | 项目初始化与基础架构 | KID-01 | 接入萤石开放平台获取设备列表、RTSP/HLS 拉流地址、云台控制 | `src/ezviz/client.py`（EzvizClient 类 501 行：Token 管理+设备列表+HLS/RTSP/RTMP/FLV 流+PTZ 云台，异步 httpx，指数退避重试）；`src/ezviz/__init__.py`；`scripts/test_ezviz.py`（验证脚本）；`configs/ezviz.yaml`（含真实 appKey+5 激活码，已 gitignore）；`src/utils/config.py` 新增 `get_ezviz_config()`；Token 获取已验证通过 |
| T1.4 | 视频流接入与帧提取模块 | 项目初始化与基础架构 | — | VideoCapture 类支持 RTSP/本地文件/摄像头三种源，帧采样降频（15fps→10fps），帧缓冲区（deque）用于视频回溯 | src/data/video_capture.py |
| T1.5 | 配置管理与日志系统 | 项目初始化与基础架构 | — | config.py 基于 OmegaConf 读取 base.yaml 单例配置 + get_ezviz_config()；logger.py 基于 loguru 按天轮转+控制台彩色+错误专属日志，幂等初始化 | src/utils/config.py + src/utils/logger.py |
| T2.1 | 视频采集脚本 | 数据采集管线 | — | collect_single() 参数化采集单段视频+元数据 JSON；collect_batch() 读 CSV 计划批量采集 | scripts/collect_video.py |
| T2.2 | 数据预处理管线 | 数据采集管线 | — | ROICropper 人体框外扩 20%+resize 256×256+padding 黑边；PreprocessPipeline 整合检测+裁剪；批处理脚本 | src/data/preprocess.py + scripts/preprocess_videos.py |
| T2.3 | 骨骼关键点提取管线 | 数据采集管线 | — | KeypointStore 时序关键点存 .npy（T,33,4）+元数据 JSON；批量提取脚本 | src/data/keypoint_store.py + scripts/extract_keypoints.py |
| T2.4 | 标注工具配置与数据管理 | 数据采集管线 | 石旗开 | 搭建 LabelStudio 标注平台，定义跌倒风险等级标注规范，管理标注数据集 | `docs/labelstudio.md`（XML 标注配置：33 关键点标签+4 级风险选择）；`scripts/labelstudio_import.py`（关键点 JSON/视频→LabelStudio 任务 JSON，含预标注）；`scripts/labelstudio_export.py`（导出→训练标注格式）；`scripts/labelstudio_agreement.py`（Cohen's kappa+逐关键点一致率）；`tests/test_labelstudio.py`（46 个测试） |
| T2.5 | PyTorch Dataset/DataLoader | 数据采集管线 | — | FallRiskDataset 继承 Dataset 返回 {keypoints,risk_score,risk_level}；时序采样 T=90；数据增强（抖动+翻转+裁剪+时间缩放）；create_dataloaders 按 7:1.5:1.5 划分。注：关键点维度为 33 | src/data/dataset.py |
| T2.6 | 数据版本管理（DVC） | 数据采集管线 | 石旗开 | 用 DVC 管理视频/关键点数据集版本，支持实验复现 | `.dvc/` 元数据 + `data.dvc`（data/ 4 文件共 55.3MB）；本地默认 remote `storage`→`dvc-storage/`（gitignored）；`docs/dvc.md` 使用指南；pyproject.toml dev 依赖添加 dvc |
| T3.1 | 姿态估计推理引擎 | 姿态估计与步态特征提取 | 石旗开 | 人体姿态估计推理封装，支持 mediapipe/yolo_pose 后端切换，输出统一 (33,4) 关键点格式 | `src/data/yolo_pose_extractor.py`（YoloPoseExtractor：YOLOv8n-pose 懒加载+自动下载，COCO 17→MP 33 映射，归一化输出）；`src/utils/keypoints.py`（COCO_TO_MEDIAPIPES + convert_coco_to_mediapipe）；`src/data/keypoint_extractor.py`（create_keypoint_extractor 工厂函数）；`configs/base.yaml`（pose_estimation.backend 字段，默认 mediapipe）；monitor 按后端跳过冗余人体检测；`tests/test_yolo_pose_extractor.py`（14 个测试） |
| T3.2 | 步态特征提取器 | 姿态估计与步态特征提取 | — | 四大相对特征：行走节拍（髋 y 坐标 FFT 主频）、步幅幅度（踝摆动/躯干高度归一化）、躯干稳定指数（肩髋连线夹角变化）、活动密度（髋部位移占比） | src/inference/features.py |
| T3.3 | 步态特征有效性验证 | 姿态估计与步态特征提取 | 石旗开 | 用 t-SNE 可视化、SHAP 解释、统计检验验证四大步态特征对风险预测的有效性 | `scripts/validate_features.py`（统计分布/t-SNE/SHAP/相关性分析）；`tests/test_validate_features.py`（18 个测试）；产出 `docs/figures/` 3 张图（correlation_matrix.png、shap_importance.png、tsne_feature_space.png） |
| T7.1 | 预警决策引擎 | 分级预警引擎 | — | 四级预警：低风险→关注级→预警级→高危级；支持注册响应动作、事件日志 | src/alerts/engine.py |
| T7.3 | 预警推送服务 | 分级预警引擎 | — | ConnectionManager 连接管理器，/ws/alerts 端点实时推送告警到前端，心跳 ping/pong 机制 | src/api/websocket.py |
| T8.1 | FastAPI 应用骨架与路由设计 | 后端服务 | — | main.py（lifecycle+CORS+路由注册+WebSocket+静态文件）；routes.py 拆分 monitor/alerts/stats 三个 APIRouter | src/api/main.py + src/api/routes.py |
| T8.3 | 数据持久化 | 后端服务 | — | Database 单例，SQLite 两表 risk_records+alert_events；monitor._run() 阶段8 调用 insert_risk_record()/insert_alert_event()，含 try/except 异常处理 | src/api/database.py + src/inference/monitor.py（阶段8 持久化）|
| T9.1 | 家属端实时风险看板 | 前端展示界面 | KID-01 | React+ECharts：风险等级大卡片、仪表盘、趋势折线图、告警列表；WebSocket 实时连接+5 秒刷新；2026-07-22 视觉重设计（暗色模式/动画/渐变条/适老化） | frontend/src/App.jsx + index.css |
| T11.1 | 单元测试 | 测试与部署 | — | tests/ 共 6 个文件 138 个测试：test_core.py(52)+test_labelstudio.py(46)+test_validate_features.py(18)+test_yolo_pose_extractor.py(14)+test_smoke.py(5)+test_integration_monitor.py(3) | tests/ 目录 |

---

## 二、部分完成任务（11个）

> 说明：以下任务代码框架已就绪，但模型训练处于训练集采集+算法迭代阶段，尚未产出可用权重；或依赖未完成模块/外部条件，无法完整运行验证。

| 任务ID | 任务名称 | 所属模块 | 负责人 | 任务细节 | 已完成部分 | 未完成部分 |
|--------|---------|---------|--------|---------|-----------|-----------|
| T5.1 | 时序特征编码器 | 跌倒风险预测模型 | 王蔚怀 | Transformer Encoder 时序特征提取（4 层 8 头 d_model=256，CLS token+位置编码） | src/models/temporal_encoder.py 代码就绪，TemporalEncoder 类已实现，可前向传播 | **训练集采集与算法迭代中**：编码器结构可能随训练实验调整；未经验证收敛性；无训练权重产出 |
| T5.2 | 多模态融合模块 | 跌倒风险预测模型 | 王蔚怀 | 三路 Cross-Attention 融合（时序全局+步态手工+环境风险），残差+FFN+LayerNorm | src/models/multimodal_fusion.py 代码就绪，MultiModalFusion 类已实现 | **算法迭代中**：环境风险输入因 T4.x 未完成实际无法使用（5 维特征缺失）；融合策略待训练验证；无训练权重产出 |
| T5.3 | 风险评分回归头 + 分类头 | 跌倒风险预测模型 | 王蔚怀 | 回归头 MLP+Sigmoid→[0,100]、分类头 4 级、OrdinalLoss 序数损失、MultiTaskLoss 多任务加权 | src/models/risk_head.py 代码就绪，RiskPredictionHead/OrdinalLoss/MultiTaskLoss 已实现 | **算法迭代中**：多任务损失权重 α/β/γ 待调优；序数损失有效性待训练验证；无训练权重产出 |
| T5.4 | 完整模型 FallRiskPredictor | 跌倒风险预测模型 | 王蔚怀 | 组装 Encoder+Fusion+Head 端到端模型，forward 输出 {risk_score,risk_probs,risk_logits} | src/models/fall_risk_predictor.py 代码就绪，FallRiskPredictor 类已实现 | **算法迭代中**：模型整体结构可能随训练实验调整；未经验证收敛性；无训练权重产出 |
| T5.5 | 训练管线 | 跌倒风险预测模型 | 王蔚怀 | AdamW+CosineAnnealingWarmRestarts+混合精度+早停+Checkpoint 完整训练循环 | scripts/train.py 代码就绪，训练/验证/测试循环已实现 | **训练集采集与算法迭代中**：训练数据集正在采集；实际训练未执行；无 best.pt 权重产出（checkpoints/ 目录不存在）；训练超参待调优 |
| T5.6 | 模型评估脚本 | 跌倒风险预测模型 | 王蔚怀 | 模型训练后的评估脚本（MAE/AUC/混淆矩阵/分级准确率等指标） | 评估脚本框架已编写 | 更新日志明确写"部分编写被中断，待补全"；且模型未训练完成，无权重可评估 |
| T8.2 | 推理服务封装 | 后端服务 | 王蔚怀 | FallRiskMonitor 单例封装，线程化运行，整合视频→检测→关键点→过滤→特征→基线→偏离→预警→持久化全链路 | src/inference/monitor.py 代码就绪，FallRiskMonitor 单例已实现；阶段8 持久化+异常处理+device_id 已补全 | **推理输出仍无效**：依赖 T5.5 产出的模型权重，当前模型未训练无权重，推理结果无意义 |
| T11.2 | 集成测试与端到端测试 | 测试与部署 | 孟鑫杰 | 真实视频流→检测→关键点→特征→基线→偏离→预警→推送的完整链路验证 | tests/test_integration_monitor.py（3 个测试）用**合成关键点帧**注入，验证算法管线接通+持久化落盘 | **真实端到端未完成**：萤石设备未激活无真实视频流，模型未训练无有效推理输出，无法验证完整链路；合成数据测试不等同端到端测试 |
| T11.3 | 性能基准测试 | 测试与部署 | 孟鑫杰 | 端到端延迟、吞吐量、CPU/内存占用等性能指标基准测试 | tests/ 下有 138 个功能测试可作基础 | **全项目无性能基准测试脚本**（搜索 benchmark/perf_test 无匹配）；且模型未训练，性能指标无意义 |
| T11.4 | Docker 容器化部署 | 测试与部署 | 孟鑫杰 | 容器化打包部署，三服务编排（api+redis+nginx），生产环境一键启动 | docker/Dockerfile（python:3.12-slim+OpenCV/MediaPipe 依赖）+ docker-compose.yml（三服务）+ nginx.conf（反向代理）文件就绪 | **未经实际运行验证**：日志无 docker-compose up 成功记录；萤石设备未激活容器内无法拉真实流；模型无权重推理无效；文件就绪 ≠ 部署完成 |
| T10.2 | 数据安全与合规文档 | 隐私保护与边缘计算 | 孟鑫杰 | 数据安全方案文档、隐私合规说明（符合个人信息保护法） | 已编写 `docs/数据安全与合规说明.md`：数据流图、边缘架构说明、合规分析、安全措施、知情同意机制 | 待 T10.1 边缘计算落地后同步更新；人脸模糊/数据库加密等待实施 |

---

## 三、未开始任务（12个）

| 任务ID | 任务名称 | 所属模块 | 负责人 | 任务细节 | 未开始原因 |
|--------|---------|---------|--------|---------|-----------|
| T4.1 | 室内物体检测与语义分割 | 环境风险感知模块 | 王蔚怀 | 检测室内障碍物（椅子/桌子/门槛/地毯等），语义分割地面/墙面区域 | src/edge/ 仅有空 __init__.py，无任何实现代码 |
| T4.2 | 地面区域分析与照明评估 | 环境风险感知模块 | 王蔚怀 | 分析地面平整度/湿滑度，评估环境光照强度，识别低照度风险 | 同上 |
| T4.3 | 环境风险评分模块 | 环境风险感知模块 | 王蔚怀 | 综合物体/地面/照明因素计算环境风险评分（5 维特征），供 T5.2 多模态融合使用 | 同上；注：T5.2 融合模块已写但环境特征输入缺失 |
| T5.7 | 模型导出与优化（ONNX/INT8） | 跌倒风险预测模型 | 王蔚怀 | 将 FallRiskPredictor 导出 ONNX，INT8 量化压缩，部署推理加速 | 无导出脚本；且模型未训练无权重可导出 |
| T6.1 | 跌倒动作分类器（LSTM） | 跌倒动作实时检测 | 王蔚怀 | LSTM 分类器识别跌倒动作（区别于风险预测），实时检测近似跌倒触发高危告警 | 无 LSTM 分类器代码 |
| T7.2 | 告警去重与防抖 | 分级预警引擎 | 张阳川 | 告警状态机去重，防止短时间内重复告警刷屏，支持告警升级/降级流转 | engine.py 无去重状态机 |
| T9.2 | 管理后台数据面板 | 前端展示界面 | 张阳川 | 管理端数据面板：设备管理、历史告警查询、趋势统计、用户管理 | frontend/ 仅有家属端看板，无管理后台 |
| T10.1 | 边缘计算架构实现 | 隐私保护与边缘计算 | 孟鑫杰 | 视频流本地边缘推理，不上传云端，保护隐私；EdgeProcessor 类 | src/edge/ 仅有空 __init__.py；依赖 T5.7 ONNX 导出 |
| T12.1 | 技术开发文档 | 技术文档与作品提交 | 待定 | 完整技术开发文档：架构设计、算法原理、接口说明、部署指南 | docs/guide.md、docs/tips.md 已有部分内容，但完整技术开发文档未成稿 |
| T12.2 | 测试报告与验证材料 | 技术文档与作品提交 | 待定 | 正式测试报告：单元测试覆盖率、集成测试结果、性能测试数据 | 依赖 T11.2/T11.3 完成 |
| T12.3 | 演示视频录制 | 技术文档与作品提交 | 待定 | 系统演示视频：功能展示、典型场景、告警流程 | 依赖核心功能可用（模型训练完成） |
| T12.4 | 最终打包与提交 | 技术文档与作品提交 | 待定 | 按挑战杯要求打包源码+文档+视频+演示材料，最终提交 | 依赖前面任务完成 |

---

## 四、重要提醒

1. **T5.1~T5.5 模型代码已就绪，但训练处于训练集采集+算法迭代阶段**——`TemporalEncoder`/`MultiModalFusion`/`RiskPredictionHead`/`FallRiskPredictor`/`train.py` 代码均已实现，但实际训练数据集正在采集，算法在迭代中，尚未产出 `best.pt` 权重（`checkpoints/` 目录不存在）。这是当前最大阻塞：模型无权重 → T8.2 推理输出无效 → T11.2 端到端测试无被测对象 → T11.3 性能测试无意义 → T11.4 部署无法验证 → T12.2 测试报告无法撰写。

2. **已完成任务 ≠ 可用状态**——部分完成任务依赖未完成任务，例如 T8.2 推理服务依赖 T5.5 产出的权重（持久化+异常处理已补全但输出仍无效），T5.2 多模态融合依赖 T4.3 环境特征输入，目前均无法输出有效结果。

3. **T1.3 萤石代码已就绪，待设备激活**——`EzvizClient` 类已实现（501 行，含 Token 管理/设备列表/HLS/RTSP 流/云台控制），`configs/ezviz.yaml` 已含真实 appKey+5 个激活码（已 gitignore），Token 获取已验证通过。**阻塞项：账号下无摄像头设备，需用激活码激活套餐拿到 deviceSerial 后才能获取真实 RTSP 流地址**。激活后即可拉流，无需改代码。

4. **T4.1~T4.3 完全空白**——环境风险感知模块缺失（`src/edge/` 仅有空 `__init__.py`），导致 T5.2 多模态融合的环境特征输入（5 维）无法使用，模型架构名不副实。

5. **T3.1 YOLO-Pose 方案已落地**——`src/data/yolo_pose_extractor.py`（YoloPoseExtractor：YOLOv8n-pose 懒加载+自动下载，COCO 17→MP 33 映射）、`src/utils/keypoints.py`（COCO_TO_MEDIAPIPE + convert_coco_to_mediapipe）、`src/data/keypoint_extractor.py`（create_keypoint_extractor 工厂函数）、`configs/base.yaml`（pose_estimation.backend 字段，默认 mediapipe 可切换 yolo_pose）、monitor 按后端跳过冗余人体检测。14 个测试通过。

6. **⚠️ T11.2/T11.3/T11.4 测试与部署均未实质完成**——T11.2 集成测试仅用合成数据非真实端到端（3 个测试）；T11.3 无性能基准脚本；T11.4 Docker 仅有文件未经实际运行验证。三者共同根因是模型未训练+萤石设备未激活，导致测试与部署缺乏被测对象。

7. **⚠️ T8.2 推理服务输出无效**——`FallRiskMonitor` 单例代码就绪可启动，持久化阶段已补全，但依赖 T5.5 产出的模型权重，当前模型未训练无权重，推理结果无意义。

8. **测试覆盖已大幅提升**——tests/ 共 6 个文件 138 个测试（test_core 52 + test_labelstudio 46 + test_validate_features 18 + test_yolo_pose_extractor 14 + test_smoke 5 + test_integration_monitor 3），较 7 月初的 60 个测试翻倍。

9. **王蔚怀任务最重**——部分完成 7 个（T5.1~T5.6 + T8.2）+ 未开始 6 个（T4.1/T4.2/T4.3/T5.7/T6.1），建议优先排期或拆分。
