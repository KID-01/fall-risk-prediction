# 本机启动与萤石联调指南

本指南仅用于本机联调。不要将 AppKey、AppSecret、AccessToken、设备激活码或直播地址写入 Git、文档、截图或聊天记录。

## 1. 准备 Python 环境

在 PowerShell 中进入项目根目录：

```powershell
cd D:\TIAOZHANBEI\fall-risk-prediction
.\venv\Scripts\Activate.ps1
$env:PYTHONIOENCODING = "utf-8"
```

如果激活命令提示找不到文件，先确认当前目录下存在 `venv`，本项目使用的是 `venv` 而不是 `.venv`。

## 2. 配置萤石开放平台密钥

在萤石开放平台的“控制台 -> 我的应用 -> 应用详情”获取新的 AppKey 和 AppSecret。创建仅保存在本机的配置文件：

```powershell
notepad configs\ezviz.yaml
```

填写以下内容：

```yaml
ezviz:
  app_key: "你的 AppKey"
  app_secret: "你的 AppSecret"
```

`configs/ezviz.yaml` 已被 `.gitignore` 忽略，不应提交。客户端会自动获取 AccessToken，无需手工填写。

## 3. 验证萤石平台和设备

```powershell
venv\Scripts\python.exe scripts\test_ezviz.py
```

该脚本依次验证配置读取、AccessToken 获取、设备列表和首台设备的 HLS 地址获取。设备未开机、未联网或没有直播权限时，设备列表或直播地址步骤可能失败；这时不能进行真实视频流验证。

## 4. 启动后端

新开终端 A：

```powershell
cd D:\TIAOZHANBEI\fall-risk-prediction
.\venv\Scripts\Activate.ps1
$env:PYTHONIOENCODING = "utf-8"
venv\Scripts\python.exe -m uvicorn src.api.main:app --host 0.0.0.0 --port 8000
```

另开终端检查健康状态：

```powershell
Invoke-RestMethod http://127.0.0.1:8000/health
```

预期返回 `status: healthy`。

## 5. 启动前端

新开终端 B：

```powershell
cd D:\TIAOZHANBEI\fall-risk-prediction\frontend
npm run dev
```

如果系统找不到 `npm`：

```powershell
$env:Path = "D:\software\Nodejs;" + $env:Path
npm run dev
```

在浏览器打开终端显示的地址，通常是 `http://localhost:3000`。

## 6. 设备在线后启动 RTMP 分析

以下命令自动查询第一台在线设备、获取短期 RTMP 地址并请求后端启动分析。命令不会输出直播地址：

```powershell
cd D:\TIAOZHANBEI\fall-risk-prediction
.\venv\Scripts\Activate.ps1
@'
import asyncio
import httpx
from src.ezviz.client import create_client_from_config

async def main():
    client = create_client_from_config()
    try:
        devices = await client.list_devices()
        device = next((item for item in devices if item.get("status") == 1), None)
        if device is None:
            print("没有在线设备，请确认摄像头开机并联网。")
            return

        rtmp = await client.get_live_stream(
            device["deviceSerial"], channel_no=1, protocol=2
        )
        if not rtmp:
            print("未获取到 RTMP 地址，请检查直播权限。")
            return

        response = httpx.post(
            "http://127.0.0.1:8000/api/v1/stream/start",
            json={
                "source": rtmp,
                "person_id": "elder-001",
                "device_id": "ezviz-camera-1",
            },
            timeout=20,
        )
        print(response.status_code, response.json().get("message"))
    finally:
        await client.close()

asyncio.run(main())
'@ | venv\Scripts\python.exe -
```

如果有多台在线设备，应根据 `scripts/test_ezviz.py` 返回的设备列表明确指定目标设备，不要默认使用第一台。

## 7. 检查监控状态

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/v1/risk/current | ConvertTo-Json -Depth 5
```

重点字段：

- `frames_processed` 持续增长：后端正在拉取视频帧。
- `frames_valid` 增长：人体姿态关键点提取和帧过滤通过。
- `baseline_samples` 增长：正在采集个人正常步态基线。
- `baseline_ready` 为 `true`：基线完成，可开始风险评估。
- `current_risk_label`：当前基线或风险状态。

当前前端展示风险看板、趋势和告警；现有实现不提供萤石实时画面预览。

## 8. 停止监控

```powershell
Invoke-RestMethod -Method Post http://127.0.0.1:8000/api/v1/stream/stop
```

## 9. 无设备阶段可完成的检查

```powershell
$env:PYTHONIOENCODING = "utf-8"
venv\Scripts\python.exe scripts\test_ezviz.py
venv\Scripts\python.exe -m pytest tests -q
```

在摄像头恢复在线前，验收范围仅包括配置读取、Token、设备列表和本地视频/测试链路；RTMP 拉帧、有效关键点、基线增长和真实看板数据必须在设备在线后验证。
