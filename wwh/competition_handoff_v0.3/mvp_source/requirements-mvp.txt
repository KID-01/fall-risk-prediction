# Fall Risk MVP v0.1 — 最小运行依赖（基于实际验证环境；勿强制升级/dependency churn）
# 验证解释器：E:\anaconda\conda_envs\yolo\python.exe（Python 3.10.20；队友机器路径可不同）
numpy>=2.2
opencv-python>=5.0
pillow>=12
pyyaml>=6
torch==2.13.0+cpu
ultralytics==8.4.96
# 模型文件（不在包内，需按 README 放置）：
#   weights/pose/yolo26n-pose.pt  (pose/motion 分支)
#   yolo26n.pt                    (COCO 预训练；env 分支)