# 后端对云端小程序：阻塞点清单

更新时间：2026-08-30

## 当前结论

本地 Python 后端运行正常，风险通知适配器已经具备向 CloudBase 发起 HTTP 请求的能力。当前小程序告警链路尚未完成验收，阻塞发生在公网云函数地址和 CloudBase 配置侧。

目前确认的完整 CloudBase 地址为：

```text
https://cloud1-d2gl1lav2eb6e440e-1477389215.ap-shanghai.app.tcloudbase.com/fallAlarmPush
```

后端默认配置和一键启动脚本已同步为该地址。此前 `shturl.cc` 短链接不再使用。

## 已确认正常

| 项目 | 结果 |
| --- | --- |
| FastAPI `/health` | 返回 `{"status":"healthy"}` |
| 通知策略接口 | 可访问，关注级/高危级包含 `cloud_function` |
| 本地告警记录 | 网关失败时仍会保留，不会因云端失败丢失 |
| 请求方式 | 后端使用 `POST` JSON，支持 `action=push` 等契约 |

## 最新云端实测

使用标记为 `codex-test` 的测试告警完成完整闭环：

| 动作 | 结果 |
| --- | --- |
| `action=pull` | HTTP 200，`code=0`，可读取已有记录 |
| `action=push` | `code=0`，返回“告警已入库” |
| 再次 `pull` | 能读取新建的 `codex-test` 记录，`isRead=false` |
| `action=confirm` | `code=0`，返回“确认成功” |

这证明当前完整 CloudBase 地址、云函数路由、数据库写入/读取和确认接口均可用。测试记录 ID 为 `2612439e6a93d6c6012aa01b164393e5`。

## 当前阻塞点

### 1. 短链接无法解析（已解决）

此前对短链接发起请求时，本机返回：

```text
不知道这样的主机。 (shturl.cc:443)
```

该问题已通过使用完整 CloudBase 地址绕过。当前完整地址实测返回 HTTP 200、`code=0`，网关和函数已可访问。

### 2. CloudBase 环境与公网地址可能不一致（已核实）

当前提供的环境 ID 为：

```text
cloud1-d2gl1lav2eb6e440e
```

仓库早期文档记录的是其他环境 ID，不能继续混用。当前完整地址对应环境 `cloud1-d2gl1lav2eb6e440e`，已通过 `pull` 请求验证。

### 3. HTTP 触发器/函数发布状态（已核实）

已通过 `pull/push/confirm` 实测确认以下配置有效：

- 云函数名称为 `fallAlarmPush`；
- 函数已发布，而非仅保存代码；
- 已开启 HTTP 公网触发器；
- 路径为 `/fallAlarmPush`；
- 触发器绑定到正确环境；
- 函数具备访问 `fall_alerts` 数据库集合的权限。

### 4. 生产安全与真实告警验收仍待完成

- 当前只验证了测试告警，尚未用真实风险事件验证后端自动推送；
- 生产网关仍需鉴权、限流、来源校验和 HTTPS 策略；
- 需要小程序真机确认轮询、告警展示和用户确认交互；
- AppKey、Secret、AccessToken 等敏感信息不得进入小程序源码或 Git。

### 5. 小程序端正式验收材料仍待补齐

虽然接口已完成技术验证，但正式交付仍建议由云函数/小程序团队补充以下材料：

- `action=pull` 返回 HTTP 200 且 `code=0`；
- `action=push` 成功写入一条告警；
- `action=confirm` 能将记录标记为已读；
- 函数版本、环境 ID、部署时间和运行日志。

## 责任边界

| 现象 | 主要责任方 | 处理方向 |
| --- | --- | --- |
| `shturl.cc` 无法解析 | 网络/短链接提供方 | 提供可解析域名或完整 URL |
| HTTP 404 | CloudBase 触发器 | 检查 `/fallAlarmPush` 路径和发布状态 |
| `INVALID_ENV` | CloudBase 环境配置 | 核对环境 ID 和 URL 绑定关系 |
| HTTP 200 但 `code` 非 0 | 云函数代码/数据库 | 查看函数日志和集合权限 |
| 后端 `cloud_push=failed` | 网关不可用或后端环境变量 | 检查 URL、启用开关并重启后端 |
| 小程序请求本地 `127.0.0.1` | 小程序配置 | 改用公网 HTTPS 网关 |

## 解除阻塞后的验证顺序

1. 云函数方提供完整 HTTPS URL，并确认环境 ID。
2. 使用 `POST {"action":"pull","limit":10}` 验证 HTTP 200、`code=0`。
3. 使用 hybrid 格式执行一次 `action=push`，确认 `fall_alerts` 新增记录。
4. 后端设置 `WECHAT_FALL_ALARM_PUSH_ENABLED=1`、新 URL 和 `payload_mode=hybrid`，重启服务。
5. 触发关注级风险，确认后端通知详情为 `cloud_push.status=sent`。
6. 小程序轮询并显示告警，再验证 `confirm` 幂等确认。

## 安全提醒

云函数 URL、AppKey、Secret、AccessToken 不应写入小程序源码或公开仓库。生产环境还需要鉴权、限流、来源校验和 HTTPS。
